<h1>Livros disponiveis</h1>

<div class="books-container">
    <a href="/">Adicionar livro</a>
  {{#each books}}
    <div>#{{this.id}} - <span class="bold">{{this.title}} - {{this.pageqty}} Páginas</span> </div>
  {{/each}}
</div>